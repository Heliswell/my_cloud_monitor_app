apiVersion: v1
kind: Service
metadata:
  name: cloud-monitor-svc
spec:
  type: NodePort
  selector:
    app: cloud-monitor
  ports:
  - name: http
    port: 80
    targetPort: 5000
    nodePort: 30080
